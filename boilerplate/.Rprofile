# Edit this .Rprofile at your own risk
if ( file.exists("./RunVisionEval.RData") ) {
	source("./RunVisionEval.R")
} else {
	if ( file.exists("Install-VisionEval.R") ) source("Install-VisionEval.R")
}
# This .Rprofile might be too aggressive (if you start R in this folder, it will
# immediately try to install VisionEval from what's here).  That could be good
# for a Linux or MacOS system that installs from source...
